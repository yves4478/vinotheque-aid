# Expo Go / real iPhone testing should always use the production backend.
# Replace this with the real public Coolify API domain, not the web app domain.
EXPO_PUBLIC_APP_ENV=prod
EXPO_PUBLIC_API_URL=https://api.vinotheque.ch

# Optional: package scripts can also read this for the Prod simulator.
EXPO_PUBLIC_PROD_API_URL=https://api.vinotheque.ch

# Local simulator testing uses package scripts that override the URL:
# npm run ios:dev
# npm run start:simulator:dev

# Echtes iPhone gegen lokales Backend (optional):
# EXPO_PUBLIC_API_URL=https://<ngrok-subdomain>.ngrok-free.app
# EXPO_PUBLIC_APP_ENV=dev
