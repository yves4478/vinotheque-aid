// TODO (transfer agent): copy full content from src/data/wineRegionMetadata.ts
// No modifications needed — pure data, no browser APIs.

export const wineRegionMetadata: Record<string, unknown> = {};
